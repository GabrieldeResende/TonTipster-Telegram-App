import useMatches from "@/app/hooks/use-Matches";


const BetsForFixture = (fixtureId: number) => {

    // const match = useMatches({ fixtureId: fixtureId });
    // const chinLink = Contract.getAllOpenBets(fixtureId);
    // const tableData = chinLink.map((bet) => {
    //     return (
    //         <tr key={bet.id}>
    //             <td>{bet.id}</td>
    //             <td>{bet.fixtureId}</td>
    //             <td>{bet.AddressFirstChoice}</td>
    //             <td>{bet.betValue1}</td>
    //             <td>{match[0].teams.home.id === bet.idTeamFirstChoice ? match[0].teams.home.name : match[0].teams.away.name}</td>

    //             <td>{bet.AddressSecondChoice}</td>
    //             <td>{bet.betValue2}</td>
    //             <td>{bet.idTeamSecondChoice}</td>
    //             <td>{match[0].teams.home.id === bet.idTeamSecondChoice ? match[0].teams.home.name : match[0].teams.away.name}</td>
    //         </tr>
    //     )
    // }

    return (
        <div>
            <h1></h1>
        </div>
    )
}

export default BetsForFixture;